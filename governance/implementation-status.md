# 实施状态

更新时间：2026-09-19。

## 已完成

- P0：独立 Git 项目、Node/DSH 版本锁、隔离 DSH_HOME、遥测/Web 关闭、项目 Skills 根、20 份合成样本与 60 条 Recall@10 验收。
- P1：文件、消息和许可域网页接收；不可变原件、SHA-256、版本、PDF/DOCX/PPTX/HTML/VTT/JSON 解析、中文检索、结论与可验证引用。
- P2：版本替代、冲突、持久任务与重试；音视频人工转写及关键帧挂接，时间锚点回到媒体原件；当时尚未实现的自动 OCR/ASR 明确保持关闭，后由 P6 补齐。
- P3：概念/别名/关系、记忆候选/确认/撤销/替换、工作日志、学习日志和复习日期；关键变更写入审计。
- P4：本机真实数据看板、CSRF/CSP、完整性检查、JSONL 导出、在线 SQLite 一致备份、空目录恢复演练、成本用量台账、删除影响预览与确认删除、注入/越权/恢复回归、P50/P95 本地检索基线。
- P5：阶段课程、课节、白话词典、服务端测验、本地笔记与进度；安全迁移器区分合成/脱敏/内部内容，并把旧来源定位保留为“待映射”而非引用。
- 解析增强：XLSX 保留工作表与单元格范围；PPTX 内嵌图片按幻灯片落盘并进行哈希完整性检查。
- P6：SiliconFlow OCR 与 ASR 受项目许可、域名白名单、本机显式开关和环境密钥四项约束；外部运行状态、模型、追踪 ID、失败与用量进入审计，密钥不持久化。
- DSH：`pet_learning` stdio MCP 已进入展开配置；证据检索、学习路径、候选记忆、外部处理状态以及受控 OCR/ASR 工具发现通过独立 MCP 客户端测试。

## 有意保持关闭

- `realCompanyDataApproved=false`，外部传输默认禁止；不得导入真实公司或病例资料。
- `dsh-knowledge@0.4.0` 候选因两条 High 依赖告警和 AGPL 许可审查保持 quarantine，未注册启用。
- OCR/ASR 实现已存在但默认门禁关闭；embedding、rerank 与自动通知仍未获授权，不在后台悄悄运行。

## 待部署环境验证

- 使用获批模型凭据启动一次真实 DSH 会话，观察 AGENTS 哨兵、Skills 列表与 `mcp__pet_learning__*` 工具是否同时进入模型上下文。
- 在资料负责人和专业审核人就位后，另行批准真实数据范围、保留期、备份位置和临床高风险审核流程。

上述两项是上线授权门，不影响脱敏/虚构资料的本地工程闭环。
